export class CreateVirtualCourseUI {

  static btnFromTemplate = '.modal-content button i.icon-copy'
  static btnNew = '.modal-content button i.fa-plus-circle'
  static courseName = '#menu-web div.edv-title-course a.open-body'
  // Este es un selector de componen, por lo que si bien no se usan en el spet definido, sería de este componente
  static titleVirtualCourse = '#section-body h5'
}